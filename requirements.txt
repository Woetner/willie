# Keep this list short: the Pi 3 A+ has 512 MB RAM (D21). State the RSS of anything new.
fastapi>=0.110     # dashboard process only (on demand)
uvicorn>=0.29      # dashboard process only
pyyaml>=6.0
psutil>=5.9
pyserial>=3.5      # MCU link (D18)
pymicro-wakeword>=2.5  # wake word "Hey Willie" (D2): 32 MB RSS, 5 % of a core, own TFLite C lib
websockets>=13       # Gemini Live bench prototype only (~6 MB RSS while a session is open)
paho-mqtt>=2.0       # phone-app bridge in the voice process (S6): ~3 MB RSS, own network thread
