# systemd holds port 8080 and passes it to willie-dashboard.service (fd 3). If the
# service is ever stopped, the next browser connection starts it again.
[Unit]
Description=WILL-E dashboard socket

[Socket]
ListenStream=8080
Accept=no

[Install]
WantedBy=sockets.target
